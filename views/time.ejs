<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1><%= title %></h1>
    <h2>Current Second: <%= second %></h2>

    <% if (second % 2 === 0) { %>
        <ul>
        <% item.forEach((fruit) => { %>
            <li><%= fruit %></li>
        <% }) %>
        </ul>
    <% }%>
    <%- include('footer.ejs') %>
</body>
</html>
